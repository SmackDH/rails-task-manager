<h1>Edit your post 🫡</h1>

<div class="container mt-3">
  <%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.submit class: "btn btn-primary mt-2" %>
  <% end %>
  <%= link_to "Delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you suuure?"} %>
</div>

<%= link_to 'Go back', tasks_path %>
